# large binary artifacts — the authors' released lens, downloadable
# from neuronpedia/jacobian-lens on HF (see lenses/*/config.yaml)
*.pt
__pycache__/
*.pyc
# external clone of anthropics/jacobian-lens (install via pip -e, see README)
ref/
